<div dir="rtl" align="right">

# الدليل الشامل لخوارزمية الحظ العادل (FairLuck Algorithm)

هذا الدليل مصمم ليكون مرجعاً واضحاً وشاملاً يشرح كيفية عمل خوارزمية "الحظ العادل" الخاصة بنظام الهدايا والمراهنات داخل التطبيق. تم تصميم هذه الخوارزمية لضمان توازن دقيق بين أرباح التطبيق، والحفاظ على تفاعل المستخدم وحمايته من الخسائر المتتالية التي قد تؤدي لتركه للتطبيق.

---

## 1. مقدمة ومبدأ العمل (Overview & Core Mechanism)
تعتمد الخوارزمية على مبدأ **"نظام التعاطف الديناميكي" (Dynamic Pity System)** و**"الانحراف عن الربح المتوقع" (Deviation)**. بدلاً من الاعتماد المطلق على العشوائية الصماء، يقوم النظام بحساب مقدار ما خسره كل مستخدم، ويقارنه بنسبة الخسارة المستهدفة (Target Loss Rate). وإذا وجد النظام أن المستخدم يخسر باستمرار أكثر من الطبيعي، فإنه يتدخل لرفع نسبة فوزه (RTP - Return to Player) تدريجياً، وقد يجبر النظام الفوز لصالحه.

تتم هذه العملية بالكامل خلف الكواليس عند كل عملية رمي أو إهداء (Process Bet) لضمان أن تبقى أموال النظام كافية لدفع الجوائز، وفي نفس الوقت إسعاد المستخدمين.

---

## 2. المحافظ وتوزيع الأموال (Wallets & Fund Distribution)
لضمان ألا يقوم النظام بدفع جوائز أكبر من الأموال المتاحة لديه (الإفلاس)، يقوم النظام أولاً بتوزيع المبالغ المدفوعة (الرهان الصافي بعد خصم رسوم التطبيق والمستلم) على 3 محافظ افتراضية مبنية على Redis لتكون سريعة جداً.

حسب دالة `distributeBetAmount` و `WalletManager`، يتم التوزيع كالتالي:
1. **المحفظة الرئيسية (Global Vault):** تحصل على **65%** من قيمة الرهان الصافي. هذه المحفظة مسؤولة عن الدفع للجوائز والمضاعفات الصغيرة (5x, 10x, 20x).
2. **محفظة الجوائز المتوسطة (Medium Wallet):** تحصل على **15%**. ومسؤولة عن الدفع للمضاعفات المتوسطة (50x, 70x, 100x).
3. **محفظة الجاكبوت الكبرى (Jackpot Wallet):** تحصل على **20%**. ومسؤولة عن الجوائز الضخمة والنادرة (250x, 500x, 1000x).

**كيف يتحكم ذلك بالفوز؟**
إذا ربح مستخدم مضاعفاً كبيراً (مثلاً 500x)، يقوم النظام بفحص **محفظة الجاكبوت**. إذا كانت المحفظة لا تحتوي على رصيد يكفي لتغطية الجائزة، فإن النظام يتراجع تلقائياً (Fallback) ويعطي المستخدم مضاعفاً أقل يستطيع النظام دفعه، أو يلغي الفوز.

---

## 3. المتغيرات وصناع القرار في اللعبة (Key Variables)
يوجد عدد من العدادات التي يتم تخزينها في ذاكرة `Redis` السريعة لكل مستخدم على حدة، وهي التي توجه الخوارزمية:

*   **مؤشر الخسائر المتتالية (`consecutive_losses`):**
    *   يعد كم مرة خسر فيها المستخدم بشكل متتالٍ.
    *   **كيف يؤثر؟** إذا وصل لـ 5 مرات متتالية، يزيد النظام نسبة فوزه. إذا وصل إلى 15 مرة، يقوم النظام بـ *إجبار فوز (Force Win)*. إذا وصل لـ 25، تصبح نسبة الفوز 100%. بمجرد فوزه يتم تصفير العداد.
*   **عداد شفقة الجاكبوت (`jackpot_pity`):**
    *   عداد يزيد مع كل مرة يلعب فيها المستخدم ولا يربح الجاكبوت الكبير (أكثر من 250x).
    *   **كيف يؤثر؟** عندما يصل لـ 400 فما فوق، يبدأ النظام برفع فرصة فوزه بالجاكبوت. وعندما يتخطى العداد 800 و 1000 وأكثر، يجبر النظام إعطائه جاكبوت (إذا كانت المحفظة تكفي).
*   **بنك المساهمة (`contribution_bank`):**
    *   يقيس حصيلة المبالغ التي ساهم بها / خسرها المستخدم مؤخراً (يُستخدم لضمان أن اللاعب يستحق جائزة كبرى لأنه ساهم بما يكفي).
*   **قفل الاستنزاف (`drain_lock`):**
    *   يتم تفعيله لمدة 24 ساعة بمجرد أن يربح المستخدم جاكبوت كبير (مضاعف 250 فأكثر).
    *   **كيف يؤثر؟** يمنع المستخدم من الفوز بجاكبوت آخر فوراً للحفاظ على اقتصاد التطبيق (يقلل فرصته جداً).
*   **الانحراف (`deviation`):**
    *   من أهم المعادلات `(-الخسارة الفعلية - الخسارة المستهدفة ) / مجمل المراهنات`
    *   إذا كان الرقم بالسالب (المستخدم خسر بشدة)، يقوم المحرك (`ProbabilityEngine`) بزيادة فرصة الفوز بنسبة ضخمة لتعويضه.

---

## 4. الإعدادات، وأين توجد، وشرح كلٍ منها (System Settings)
الإعدادات الرئيسية التي يعتمد عليها الكود يتم إدخالها والتحكم بها عادة من **لوحة التحكم (Admin Panel)** وتُحفظ في قاعدة البيانات في جدول `fair_luck_settings` ويتم استدعاؤها عبر `FairLuckSetting::getByKey`.

**أهم الإعدادات:**
1.  **`house_edge_rate` (نسبة أو حصة الكازينو/التطبيق):**
    *   *المكان:* في جدول الإعدادات تحت مفتاح `house_edge_rate`.
    *   *الوظيفة:* هي النسبة المرجعية لربح التطبيق النهائي. مثلاً `0.20` تعني أن التطبيق يأخذ 20% كحد أدنى أو اقتطاع من مجمل ما يتم تدويره. هذا الإعداد يضمن عدم استنزاف أموال التطبيق.
2.  **`target_loss_rate` (نسبة الخسارة المستهدفة لللاعبين):**
    *   *الشرح:* وهي الـ RTP (Return To Player). توجد في ملف `DeviationCalculator`. افتراضياً تعادل `0.01` أو أكثر بقليل. تحدد رياضياً معدل الخسارة المتوقع.
3.  **إعدادات المحافظ التلقائية (Rebalancing):**
    *   تتم من خلال كلاس `WalletManager` (بنسب 60%, 20%, 10% ثابتة في الكود). إذا حدث خلل في محافظة دون أخرى، يتم نقل الأرصدة تلقائياً بينهم بحيث تكون نسبهم كافية لدفع الجوائز الصغيرة بشكل دائم.

---

## 5. محرك الاحتمالات واختيار المضاعف (Probability & Smart Multiplier)
عند كل رمية، يمر النظام بمرحلتين:

**أولاً: هل سيربح المستخدم أم لا؟ (`ProbabilityEngine`)**
*   يبدأ النظام باحتمالية أساسية مستندة للمحفظة.
*   تزداد الاحتمالية إذا كان المستخدم لديه "انحراف سالب قوي" (خسائر كثيرة).
*   وهناك **حماية المبتدئين (`beginner_protection`)**: حسابات جديدة يتم إعطاؤها دفعة احتمالية لتشجيعهم على اللعب.
*   *حماية الرصيد المنخفض:* إن كان رصيد المستخدم على وشك الانتهاء (أقل من قيمة الرهان في 20)، يتم رفع نسبة فوزه للحد الأدنى `15%`، لإبقائه داخل تجربة اللعب (Mini hits).

**ثانياً: إذا قرر النظام فوزه، كم سيكون المضاعف؟ (`MultiplierSelector`)**
*   يستخدم الكود أوزاناً (Weights) لتحديد المضاعف (5x إلى 1000x).
*   المضاعفات الصغيرة (5 و 10 و 20) تملك أوزاناً ثقيلة (2800, 1800...) فاحتمالية ظهورها أكبر شيء.
*   إذا كان رصيد `jackpot_pity` (رصيد الشفقة للجاكبوت) مرتفع جداً (تخطى 800+ وكان اللاعب قد خسر مبلغاً محترماً `hasPaidForJackpot`)، يزيد وزن مضاعفات الجاكبوت بشكل صاروخي ليضمن وقوع الجائزة الكبرى له.
*   **عامل الفوضى (`Chaos Factor`):** رقم عشوائي بين 0.85 و 1.15 يُضرب في الاحتمالات حتى لا يُمكن لأي مستخدم أو بوت من اكتشاف نمط اللعبة واختراقه أو التنبؤ بمتى سيربح بالضبط.
*   **التحقق الأخير (`validateMultiplierAvailability`):** بعد وقوع اختيار المضاعف (مثلاً وقع له 1000x)، يتم فحص رصيد التطبيق (محفظة الجاكبوت). هل تكفي للدفع؟ إن كانت الإجابة نعم، يفوز ويسحب الرصيد. إن لا، يتنازل للـ 500x ثم لـ 250x وهكذا حتى يجد محفظة بها أموال تغطي الفوز، لضمان عدم إفلاس النظام أبدًا.

---

## 6. ملخص آلية العمل خطوة بخطوة لاستخدامها كشرح أو عرض (Workflow Summary)

1.  **المستخدم يضغط "رمي / إهداء" (Bet):**
    *   يتم استقطاع الرسوم. يتم تحويل الصافي إلى مقسم المحافظ (Global, Medium, Jackpot).
2.  **أخذ اللقطة المسبقة (Snapshot):**
    *   يقرأ النظام رصيد المستخدم، كم خسر، كم دورة لعب ولم يربح جاكبوت، وهل فاز بجاكبوت قريبًا وتم قفل فوزه؟
3.  **محاسبة الخسارة والشفقة:**
    *   تُحسب فرصة فوز مبدئية.. يتم تضخيمها بحال تكرار الخسائر.
4.  **تحديد النتيجة والفوز:**
    *   دوران العجلة برقم عشوائي، إذا نجح يتم فرز الجائزة.
5.  **اختيار الجائزة وحماية الرصيد:**
    *   يتم اختيار المضاعف بذكاء، والتأكد بنسبة مليون بالمئة أن أموال النظام الموجودة في (محافظ FairLuck) قادرة على دفع الجائزة، وإلا يخفض له المضاعف.
6.  **تحديث العدادات للمرات القادمة:**
    *   يتم تصفير عداد الخسارة المتتالية عند الفوز، ويتم إضافة الفوز لتقليل رصيد الانحراف السالب، لمنع استمراره بالفوز المبالغ فيه بالدورات المقبلة.
    
## 7. القاموس المرجعي للإعدادات (Settings Reference)

تم تقسيم الإعدادات هنا إلى قسمين لتعرف ما يمكن تغييره من لوحة التحكم (الداتا بيز) وما هو ثابت برمجياً داخل الملفات.

### أولاً: إعدادات لوحة التحكم (الداتا بيز - Dashboard Settings)
تستدعى هذه الإعدادات من جدول `fair_luck_settings` عبر الدالة `FairLuckSetting::getByKey`.

1. **نسبة ربح التطبيق أو الكازينو (`house_edge_rate`)**
   - **الملف:** `app/Services/FairLuck/FairLuckService3.php`
   - **رقم السطر:** 27
2. **نسبة الخسارة المستهدفة / RTP (`target_loss_rate`)**
   - **الملف:** `app/Services/FairLuck/DeviationCalculator.php`
   - **رقم السطر:** 30
3. **المضاعفات المتاحة للعب (`available_multipliers`)**
   - **الملف:** `app/Services/FairLuck/MultiplierSelector.php`
   - **رقم السطر:** 15 و 100
4. **مضاعف تعزيز المبتدئين (`beginner_boost_multiplier`)**
   - **الملف:** `app/Services/FairLuck/BeginnerProtection.php`
   - **رقم السطر:** 22 و 28
5. **أيام حماية المبتدئ (`beginner_protection_days`)**
   - **الملف:** `app/Services/FairLuck/BeginnerProtection.php`
   - **رقم السطر:** 25
6. **عدد رميات حماية المبتدئ (`beginner_protection_bets`)**
   - **الملف:** `app/Services/FairLuck/BeginnerProtection.php`
   - **رقم السطر:** 26
7. **أقصى معدل ربح للمبتدئ (`beginner_max_profit_rate`)**
   - **الملف:** `app/Services/FairLuck/BeginnerProtection.php`
   - **رقم السطر:** 27

---

### ثانياً: الإعدادات الثابتة بالكود (Hardcoded Values)
أرقام محددة مسبقاً في الملفات وتؤثر مباشرة على مسار الخوارزمية (للتعديل عليها يجب دخول الملف ذاته).

**في ملف `app/Services/FairLuck/FairLuckService3.php`:**
- **حاجز الأمان العام (`GLOBAL_SAFETY_BUFFER`):** السطر `15` (قيمته `5000`).
- **نسبة اقتطاع الكازينو (20%):** السطر `362` (`houseEdgeCut`).
- **توزيع المحافظ الصافي للمبالغ (Distribute Net Bet):**
  - **Global Vault (65%):** السطر `1046`
  - **Jackpot Wallet (20%):** السطر `1049`
  - **Medium Wallet (15%):** السطر `1052`
- **عداد الخسائر المتتالية (`consecutive_losses`):**
  - إذا وصل `5` يبدأ التعويض: السطر `146`
  - إذا وصل `15` إجبار الفوز: السطر `153` (وكذلك السطر `48`)
  - إذا وصل `25` نسبة الفوز `100%`: السطر `157`
- **شفقة الجاكبوت (`jackpotPity`):**
  - فوق `400` رفع معدل الهدف لـ `0.18`: السطر `94`
  - فوق `600` لـ `0.22`: السطر `98`
  - فوق `800` لـ `0.30`: السطر `106`
  - فوق `1000` لـ `0.45`: السطر `110`
- **عامل الفوضى لعدم التوقع (`Chaos Factor`):** بين `85%` و `115%` : السطر `90`

**في ملف `app/Services/FairLuck/WalletManager.php`:**
- **إعادة موازنة المحافظ (Target Ratios):**
  - الأسطر من `63` إلى `67`: `0.60` للمحفظة الرئيسية، `0.20` للجاكبوت، `0.10` للمتوسطة.

**في ملف `app/Services/FairLuck/DeviationCalculator.php`:**
- **نسب الاقتطاع الأساسية للحسابات:** الأسطر من `10` إلى `12` (`APP_PROFIT_RATE = 0.10`).

**في ملف `app/Services/FairLuck/ProbabilityEngine.php`:**
- **عامل تعديل الاحتمال (`adjustmentFactor`):** السطر `16` (قيمته `2.5`).
- **الحد الأدنى لفرصة الفوز (`floor`):** السطر `37` (قيمته `0.10`).

</div>
