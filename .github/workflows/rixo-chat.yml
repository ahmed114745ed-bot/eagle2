name: SSh-rixo-chat-pipline
on:
  push:
    branches: ["rixo-chat"]
jobs:
  run_rixo_chat_hub:
    runs-on: [self-hosted]
    steps:
    - name: pull latest code
      uses: appleboy/ssh-action@v1.2.1
      with:
        host: "34.18.162.251"
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.UTDVM }}
        port: ${{ secrets.PORT }}
        command_timeout: 3m
        script: |
          cd /var/www/rixo-chat
          source /home/${{ secrets.USERNAME }}/hub.sh
          threshold=80
          current=$(df -h | awk '$NF=="/"{printf "%d", $5}' | sed 's/%//')
          if [ $current -gt $threshold ]; then
            echo "Disk space is more than $threshold% utilized"
            docker system prune -a -f && docker system prune --all --force && echo "Docker system cleaned successfully"
          else
            echo "Disk space is within acceptable limits $current% "
          fi
  run_rixo_chat_app:
    runs-on: [self-hosted]
    needs: run_rixo_chat_hub
    steps:
    - name: build and deploy rixo-chat app
      uses: appleboy/ssh-action@v1.2.1
      with:
        host: "34.18.162.251"
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.UTDVM }}
        port: ${{ secrets.PORT }}
        command_timeout: 20m
        script: |
          cd /var/www/rixo-chat
          pwd
          docker build -t rixo-chat-octa:${{ github.sha }} -f Dockerfile-octana .
          docker service update --image rixo-chat-octa:${{ github.sha }} rixo-chat-octa
          threshold=80
          current=$(df -h | awk '$NF=="/"{printf "%d", $5}' | sed 's/%//')
          if [ $current -gt $threshold ]; then
            echo "Disk space is more than $threshold% utilized"
            docker system prune -a -f && docker system prune --all --force && echo "Docker system cleaned successfully"
          else
            echo "Disk space is within acceptable limits $current% "
          fi
  run_rixo_chat_job:
    runs-on: [self-hosted]
    needs: run_rixo_chat_hub
    steps:
    - name: build and deploy rixo-chat job
      uses: appleboy/ssh-action@v1.2.1
      with:
        host: "34.18.162.251"
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.UTDVM }}
        port: ${{ secrets.PORT }}
        command_timeout: 20m
        script: |
          cd /var/www/rixo-chat
          pwd
          docker build -t rixo-chat-job:${{ github.sha }} -f Dockerfile-jobs .
          docker service update --image rixo-chat-job:${{ github.sha }} rixo-chat-job
