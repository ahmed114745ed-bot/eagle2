name: Deploy Jo
on:
  push:
    branches: ["jo"]
concurrency:
  group: jo-deploy
  cancel-in-progress: false

jobs:
  hub:
    runs-on: [self-hosted]
    steps:
    - name: Pull code and rotate logs
      uses: appleboy/ssh-action@v1.2.1
      with:
        host: "34.18.156.128"
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.UTDVM }}
        port: ${{ secrets.PORT }}
        command_timeout: 5m
        script: |
          cd /var/www/jo
          source /home/${{ secrets.USERNAME }}/scripts/hub.sh || source /home/${{ secrets.USERNAME }}/hub.sh
          mkdir -p storage/jobs-logs
          chmod -R 777 storage/jobs-logs
          test -f public/settings.json || echo '{}' > public/settings.json
          test -f gcp.json || echo '{}' > gcp.json
          LOG_FILE=storage/logs/laravel.log
          if [ -f "$LOG_FILE" ]; then
            LOG_SIZE=$(stat -c%s "$LOG_FILE" 2>/dev/null || echo 0)
            if [ "$LOG_SIZE" -gt 104857600 ]; then
              truncate -s 0 "$LOG_FILE"
            fi
          fi
          find storage/jobs-logs -name '*.log' -size +50M -exec truncate -s 0 {} \; 2>/dev/null
          threshold=80
          current=$(df -h | awk '$NF=="/"{printf "%d", $5}' | sed 's/%//')
          if [ $current -gt $threshold ]; then
            sudo docker system prune -a -f 2>/dev/null
          fi

  migrate:
    runs-on: [self-hosted]
    needs: hub
    steps:
    - name: Run migrations
      uses: appleboy/ssh-action@v1.2.1
      with:
        host: "34.18.156.128"
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.UTDVM }}
        port: ${{ secrets.PORT }}
        command_timeout: 5m
        script: |
          cd /var/www/jo
          CONTAINER_ID=$(sudo docker ps --filter "name=jo-octa.1" --format "{{.ID}}" | head -n 1)
          if [ -n "$CONTAINER_ID" ]; then
            sudo docker exec "$CONTAINER_ID" php /app/artisan migrate --force || echo "Migration warning (non-fatal)"
          fi

  deploy-app:
    runs-on: [self-hosted]
    needs: [hub, migrate]
    steps:
    - name: Build and deploy app
      uses: appleboy/ssh-action@v1.2.1
      with:
        host: "34.18.156.128"
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.UTDVM }}
        port: ${{ secrets.PORT }}
        command_timeout: 20m
        script: |
          cd /var/www/jo
          sudo docker build -t jo:${{ github.sha }} -f Dockerfile-octana .
          for attempt in 1 2 3; do
            echo "Attempt $attempt: updating jo-octa..."
            if sudo docker service update \
              --image jo:${{ github.sha }} \
              --update-order start-first \
              --update-parallelism 1 \
              --update-delay 10s \
              --update-failure-action rollback \
              jo-octa; then
              break
            fi
            [ "$attempt" -eq 3 ] && exit 1
            sleep 15
          done
          sleep 10
          CONTAINER_ID=$(sudo docker ps --filter "name=jo-octa.1" --format "{{.ID}}" | head -n 1)
          if [ -n "$CONTAINER_ID" ]; then
            sudo docker exec $CONTAINER_ID php artisan whatsapp:change-token || echo "WhatsApp token skipped (non-fatal)"
          fi

  deploy-job:
    runs-on: [self-hosted]
    needs: [hub, migrate]
    steps:
    - name: Build and deploy job workers
      uses: appleboy/ssh-action@v1.2.1
      with:
        host: "34.18.156.128"
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.UTDVM }}
        port: ${{ secrets.PORT }}
        command_timeout: 20m
        script: |
          cd /var/www/jo
          sudo docker build -t jo-job:${{ github.sha }} -f Dockerfile-jobs .
          for attempt in 1 2 3; do
            echo "Attempt $attempt: updating jo-job..."
            if sudo docker service update \
              --image jo-job:${{ github.sha }} \
              --update-parallelism 1 \
              --update-delay 10s \
              --update-failure-action rollback \
              jo-job; then
              break
            fi
            [ "$attempt" -eq 3 ] && exit 1
            sleep 15
          done

  cleanup:
    runs-on: [self-hosted]
    needs: [deploy-app, deploy-job]
    if: always()
    steps:
    - name: Cleanup old images
      uses: appleboy/ssh-action@v1.2.1
      with:
        host: "34.18.156.128"
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.UTDVM }}
        port: ${{ secrets.PORT }}
        command_timeout: 5m
        script: |
          CURRENT=${{ github.sha }}
          for img in $(sudo docker images --format '{{.Repository}}:{{.Tag}}' | grep -E '^(jo|jo-job):' | grep -v "$CURRENT" | grep -v '<none>'); do
            sudo docker rmi "$img" 2>/dev/null || true
          done
          sudo docker image prune -f 2>/dev/null || true
          sudo docker builder prune -f --filter "until=24h" 2>/dev/null || true
          echo "Disk: $(df -h / | awk 'NR==2{print $5}')"
