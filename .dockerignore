# Git
.git
.github

# Dependencies (reinstalled during build)
vendor
node_modules
# TODO: remove this exclusion after backend team fixes composer.json/lock mismatch
# (intervention/image ^2.1 in json vs 3.11.8 in lock)
composer.lock

# Docker files (not needed inside the image)
Dockerfile*
docker-compose*.yml
*-deployment.yml

# Secrets and env (bind-mounted at runtime)
.env
.env.*
gcp.json
service-account.json
AuthKey_*.p8

# Tests (not needed in production)
tests

# Logs and temp files
storage/logs
storage/framework/cache
storage/framework/sessions
storage/framework/views
*.log

# IDE and OS
.idea
.vscode
*.swp
*.swo

# Documentation
*.md
LICENSE
